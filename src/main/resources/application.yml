spring:
  application:
    name: pokebase
  datasource:
    url: jdbc:h2:mem:pokebase
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2-console

